#DEV

1 Clonar el repositorio
2 Crear un .env basado en .env.template
3 Ejecutar el comando 'git submodule update --init --recursive' para reconstruir los submodulos
4 Ejecutar el comando 'docker compose up -d --build'

#Pasos para crear los Git submodulos

1 Crear un nuevo repositorio
2 Clonar el repositorio de la maquina local
3
